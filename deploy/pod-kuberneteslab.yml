apiVersion: v1
kind: Pod
metadata:
  name: kuberneteslab.kubernetes.gabrielmalakias.com
  labels:
    app: kuberneteslab
spec:
  containers:
  - name: kuberneteslab
    image: gabrielmalakias/kuberneteslab
    ports:
    - name: klab-port
      containerPort: 4567
